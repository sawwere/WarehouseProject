<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>
<body>

<table class="table caption-top table-bordered">
  <caption><h2>Список остатков на складах</h2></caption>
  <thead class="table-light">
  <tr>
    <th><%= label_tag :good, 'Товар' %></th>
    <th><%= label_tag :wh, 'Склад' %></th>
    <th><%= label_tag :quantity, 'Количество' %></th>
  </tr>
  </thead>
  <%- @goods_whs.each do |gw| %>
    <tr><td><%= "#{gw.good.nomenclature}" %></td><td><%= "#{gw.warehouse.name}" %></td><td><%= "#{gw.quantity}" %></td>
  <%-end%>
</table>

<hr>
</body>
</html>