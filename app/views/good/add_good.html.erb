<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>
<body>


<h2><caption> Добавить товар </h2></caption>
<%= bootstrap_form_for :answer, class: 'good_data_form', url: '/create_good' do |f|%>
  <div class='form-field'>
    <%=f.text_field :nomenclature, label: 'Название' %>
  </div>
  <div class='form-field'>
    <%=f.text_field :measure, label: 'Единица измерения'%>
  </div>
  <div class='form-field'>
    <%= f.submit 'Добавить' %>
  </div>
<% end -%>

<table class="table table-bordered caption-top">
  <caption><h2>Все товары</h2></caption>
  <thead>
  <tr>
    <th>Товар</th>
    <th>Единица измерения</th>
    <th>Добавить в избранное</th>
  </tr>
  </thead>


  <%= bootstrap_form_for :favourite, remote:true, url: '/create_favourites' do |f| %>
    <%- @goods.each do |g| %>
      <tr>
          <td> <%= "#{g.nomenclature}" %> </td>
          <td> <%= "#{g.measure}" %> </td>
          <td> <input type="checkbox" id="nomenclature" name="nomenclature" value=<%= "#{g.nomenclature}" %> /> </td>
      </tr>
    <%-end%>
    <th>
      <%= f.submit 'Добавить' %>
    </th>
  <% end %>

</table>

</body>
</html>
